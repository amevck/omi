json library and servalet library is in the lib folder inside web-inf
should compile all java classes sametime using *.java

eg :- C:\apache-tomcat-8.0.33\webapps\new\WEB-INF\classes>javac -cp C:\apache-tomcat-8.0.33\lib\servlet-api.jar;C:\apache-tomcat-8.0.33\lib\json-simple-1.1.1.jar *.java

then url :- http://localhost:8080/Omi/game.html

use 2 browsers(firefox chrome opera torch) and 2 private windows.
or 4 browsers.4 private windows can't use because of same session.

state diagramm is inside the zip folder too.
use enable editing in word doc if not clearly visible.